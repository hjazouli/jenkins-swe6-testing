services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins-swe6
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - .:/var/jenkins_home/workspace/SWE6-Test
    restart: always

volumes:




  jenkins_home:
